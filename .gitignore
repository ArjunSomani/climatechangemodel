# Full pristine clone of cliffgold/Optimize, kept locally as reference
# material only (has its own .git; never nest it into this repo's history).
vendor/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Bulky EIA source data + derived Parquet store. Regenerable from vendor/
# (see engine/scripts/convert_eia_to_parquet.py) or, later, from the
# scheduled EIA ingestion job (PRD §9). Specs.csv/Regions.csv are small
# and authoritative, so those ARE committed.
engine/data/eia_hourly/
engine/data/eia_parquet/

# Env / secrets
.env
.env.local

# OS cruft
.DS_Store
